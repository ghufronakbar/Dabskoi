import 'package:flutter/material.dart';

class UIColors {
  UIColors._();

  // splash screen gradient
  static const redGradient = Color.fromRGBO(192, 48, 39, 1);
  static const greyGradient = Color.fromRGBO(75, 94, 109, 1);

  // transition screen background
  static const transitionBackgroundScreen = Color.fromRGBO(242, 242, 242, 1);

  // default red
  static const mainRed = Color.fromRGBO(187, 0, 0, 1);
  static const mainWhite = Color.fromRGBO(255, 255, 255, 1);
  static const mainBlue = Color.fromRGBO(0, 119, 255, 1);

  // onboarding
  static const onboardingSubtitle = Color.fromRGBO(0, 0, 0, 1);
  static const dotColors = Color.fromRGBO(171, 171, 171, 1);

  // login
  static const loginTextField = Color.fromRGBO(238, 238, 235, 1);

  // tab
  static const tabMainBacground = Color.fromRGBO(243, 243, 243, 1);
}
